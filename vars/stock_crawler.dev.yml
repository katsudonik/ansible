---
username: vcp
anaconda_version: anaconda3-4.3.0
project_name: stock_crawler

git:
  domain: github.com
  user: katsudonik
  repository_name: stock_crawler
  key: stock_crawler
  key_path: /home/vcp/.ssh/stock_crawler
  dir: /home/vcp/{{ project_name }}
  version: "{{ git_version }}"
  owner: "{{ username }}"
  group: "{{ username }}"
  mode: 0755

vault_password_path: /home/vcp/.vault_password