---
username: vcp
anaconda_version: anaconda3-4.3.0
project_name: stock_crawler

git:
  repository_name: stock_crawler
  key: stock_crawler
  key_path: ~/.ssh/{{ git.key }}
  dir: ~/{{ project_name }}
  version: {{ git_version }
  owner: {{ username }
  group: {{ username }

vault_password_path: ~/.vault_password